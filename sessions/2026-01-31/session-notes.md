# 学习记录 - 2026-01-31

## 学习概述
- **日期**: 2026-01-31（周五）
- **时长**: 约 90 分钟
- **格式**: 系统学习 + 深入讲解
- **主要主题**: 分布式事务总结、Stream 流、Java 版本演进、Lambda 表达式、Java 11 新特性

---

## 学习内容

### 1. 分布式事务完整总结

#### 学生需求
开始新一天的学习前，先总结分布式事务的所有内容。

#### 总结内容

**五种分布式事务方案对比：**

| 方案 | 一致性 | 性能 | 复杂度 | 方法数 | 适用场景 |
|------|--------|------|--------|--------|---------|
| **TCC** | 强一致 | 高 | 高 | 3个 | 短事务、核心业务（订单+支付） |
| **SAGA** | 最终一致 | 高 | 中 | 2个 | 长事务、第三方服务（旅游预订） |
| **本地消息表** | 最终一致 | 中 | 低 | - | 简单异步（积分、通知） |
| **MQ 事务消息** | 最终一致 | 高 | 中 | - | 高并发异步（秒杀、大促） |
| **Seata AT** | 强一致 | 中 | 低 | 1个 | 数据库操作（零侵入） |

**关键记忆点：**
- TCC：Try-Confirm-Cancel，预留资源，强一致性
- SAGA：直接执行 + 补偿，不冻结资源，最终一致性
- 本地消息表：业务操作 + 插入消息在同一事务
- MQ 事务消息：半消息 + 事务回查
- Seata AT：undo_log 自动回滚，零侵入

**学生表现：** ✅ 对分布式事务的理解很好，能准确说出核心概念。

---

### 2. Stream 流详解

#### 学生困惑
"Stream 流这个我一直有很多困惑"

#### 核心概念讲解

**Stream 的本质：**
- Stream = 流水线操作
- 不是数据结构，是对数据的操作流程
- 声明式编程：说"做什么"，不说"怎么做"

**中间操作 vs 终止操作：**

| 类型 | 特点 | 返回值 | 例子 |
|------|------|--------|------|
| **中间操作** | 懒加载，不会立即执行 | Stream | `filter()`, `map()`, `sorted()` |
| **终止操作** | 触发执行，返回结果 | 具体类型 | `collect()`, `forEach()`, `count()` |

**关键理解：**
- 没有终止操作，中间操作不会执行（懒加载）
- 执行顺序：一个元素走完整个流水线，不是一个操作处理所有元素

**常用操作：**
- `filter()` - 过滤
- `map()` - 转换
- `flatMap()` - 扁平化
- `sorted()` - 排序
- `distinct()` - 去重
- `limit()` / `skip()` - 限制/跳过
- `collect()` - 收集结果
- `forEach()` - 遍历
- `count()` - 计数
- `reduce()` - 归约

**学生表现：** ✅ 能正确理解 Stream 的基本用法，知道 filter 和 map 的作用。

---

### 3. Java 版本演进

#### 学生问题
"直接讲一讲 Java 8、Java 11、Java 13、Java 17、Java 21、Java 23 以及最新版都分别有哪些比较大的改动以及建议使用的点"

#### 版本总结

**Java 8 (2014) - 里程碑版本 🔥**
- Lambda 表达式
- Stream API
- Optional 类
- 新日期时间 API
- 接口默认方法

**Java 11 (2018) - LTS 🔥**
- String 增强（`isBlank()`, `lines()`, `repeat()`, `strip()`）
- 文件读写简化
- HTTP Client（标准化）
- 直接运行 `.java` 文件

**Java 17 (2021) - LTS 🔥**
- Record 类（简化数据类）
- Switch 表达式（更简洁）
- 文本块（多行字符串）
- 模式匹配（instanceof）
- 密封类

**Java 21 (2023) - LTS 🔥**
- 虚拟线程（革命性特性）
- 模式匹配（switch）
- Record 模式匹配
- Sequenced Collections

**Java 22-24 (2024-2025) - 非 LTS**
- 预览特性，等 Java 25（下一个 LTS）

**大厂使用情况：**
- Java 8：~35%（老项目）
- Java 11：~30%（稳定项目）
- Java 17：~25%（新项目）
- Java 21：~5%（前沿项目）

**学习建议：**
1. ✅ Java 8 - 必须掌握（Lambda、Stream、Optional）
2. ✅ Java 11 - 常用（String 增强、HTTP Client）
3. ✅ Java 17 - 推荐（Record、Switch 表达式）
4. ⚠️ Java 21 - 了解（虚拟线程）

**学生表现：** ✅ 能准确回答 Java 8 最重要的三个特性（Lambda、Stream、Optional）。

---

### 4. Lambda 表达式详解

#### 学生需求
深入理解 Lambda 表达式。

#### 核心概念

**Lambda 的本质：**
- Lambda = 匿名函数 = 简化的匿名内部类
- 只能用于函数式接口（只有一个抽象方法的接口）

**基本语法：**
```java
(参数列表) -> { 方法体 }

// 例子
() -> System.out.println("Hello")           // 无参数
x -> x * 2                                   // 一个参数
(x, y) -> x + y                             // 多个参数
(x, y) -> { int sum = x + y; return sum; } // 多行代码
```

**常用函数式接口：**

| 接口 | 参数 | 返回值 | 方法 | 用途 |
|------|------|--------|------|------|
| `Runnable` | 无 | 无 | `run()` | 线程 |
| `Consumer<T>` | 1个 | 无 | `accept(T)` | `forEach` |
| `Supplier<T>` | 无 | 有 | `get()` | 延迟计算 |
| `Function<T,R>` | 1个 | 有 | `apply(T)` | `map` |
| `Predicate<T>` | 1个 | boolean | `test(T)` | `filter` |
| `BiFunction<T,U,R>` | 2个 | 有 | `apply(T,U)` | `reduce` |

**方法引用：**
- 静态方法：`Integer::parseInt`
- 实例方法：`System.out::println`
- 对象方法：`User::getName`
- 构造方法：`User::new`

**学生表现：** ✅ 理解 Lambda 的基本概念和语法。

---

### 5. Java 11 新特性详解

#### 学生需求
"String 增强（isBlank、lines、repeat）、HTTP Client（标准化）这里详细讲一下"

#### String 增强

**新增方法：**

| 方法 | 作用 | 例子 |
|------|------|------|
| `isBlank()` | 判断是否为空或只有空白 | `"  ".isBlank()` → `true` |
| `lines()` | 按行分割 | `"a\nb".lines()` → `["a", "b"]` |
| `repeat(n)` | 重复 n 次 | `"abc".repeat(3)` → `"abcabcabc"` |
| `strip()` | 去除首尾空白（Unicode） | `"  abc  ".strip()` → `"abc"` |
| `stripLeading()` | 去除开头空白 | `"  abc  ".stripLeading()` → `"abc  "` |
| `stripTrailing()` | 去除结尾空白 | `"  abc  ".stripTrailing()` → `"  abc"` |

**关键区别：**
- `isEmpty()` vs `isBlank()`：前者只判断长度，后者判断空白字符
- `trim()` vs `strip()`：前者只能去除 ASCII 空白，后者支持 Unicode

#### HTTP Client

**基本用法：**
```java
// 1. 创建 HttpClient
HttpClient client = HttpClient.newHttpClient();

// 2. 创建 HttpRequest
HttpRequest request = HttpRequest.newBuilder()
    .uri(URI.create("https://api.example.com"))
    .build();

// 3. 发送请求
HttpResponse<String> response = client.send(
    request, 
    HttpResponse.BodyHandlers.ofString()
);

// 4. 处理响应
System.out.println(response.body());
```

**特性：**
- ✅ JDK 自带，不需要第三方依赖
- ✅ 支持 HTTP/2
- ✅ 支持同步和异步
- ✅ 线程安全

**适用场景：**
- JDK 11+ 项目
- 简单的 HTTP 请求
- 不想引入第三方库

**学生表现：** ✅ 认真学习，提出了很好的问题。

---

### 6. Record 类使用方式

#### 学生问题
"那么问题是 record 是需要写一个类似的类吗，还是直接在 service 里面像属性一样创建"

#### 解答

**答案：** 需要写一个独立的类（文件）

**正确用法：**
```java
// User.java（独立文件）
public record User(String name, int age) { }

// UserService.java
@Service
public class UserService {
    public User getUser() {
        return new User("张三", 25);
    }
}
```

**关键点：**
- Record 和普通 class 一样，需要单独定义
- 自动生成构造方法、getter、equals、hashCode、toString
- getter 方法名是 `name()`，不是 `getName()`

**学生表现：** ✅ 提出了很好的问题，理解了 Record 的使用方式。

---

### 7. Java 版本使用情况

#### 学生问题
"java24 出了吗？现在大厂都是用多少 jdk"

#### 解答

**版本发布情况：**
- Java 24：预计 2025 年 3 月发布（现在是 2026 年 1 月，应该已经发布）
- Java 25：2025 年 9 月（下一个 LTS）

**大厂使用情况：**
- 阿里巴巴：Java 8, 11, 17
- 腾讯：Java 8, 11
- 字节跳动：Java 8, 11, 17
- 美团：Java 8, 11
- 京东：Java 8, 11

**版本占比：**
- Java 8：~35%（老项目）
- Java 11：~30%（稳定项目）
- Java 17：~25%（新项目）
- Java 21：~5%（前沿项目）

**为什么还在用 Java 8？**
1. 历史遗留（2014-2018 年的项目）
2. 升级成本高（测试、兼容性、培训）
3. "如果没坏，就不要修"

**升级趋势：**
- Spring Boot 3.x 要求 Java 17+
- Java 8 官方支持结束
- 新特性吸引力（Record、虚拟线程）
- 性能提升（Java 17 比 Java 8 快 10-20%）

**学生表现：** ✅ 对版本情况很感兴趣，了解了大厂的实际使用情况。

---

## 学生表现评估

### 优点
1. ✅ **学习主动性强**：主动要求总结分布式事务，提出了很多好问题
2. ✅ **理解能力好**：能准确理解核心概念（Lambda、Stream、Record）
3. ✅ **问题意识强**：提出了很多实际的问题（Record 如何使用、大厂用什么版本）
4. ✅ **知识迁移能力**：能将之前学的知识联系起来

### 需要加强
1. ⚠️ **实践经验**：建议多写代码，实际使用 Stream、Lambda、HTTP Client
2. ⚠️ **深度理解**：部分概念理解正确但不够深入（如 Record 的使用场景）

---

## 知识点掌握情况

### ✅ 已掌握

**分布式事务（高信心）**
- 五种方案对比（TCC、SAGA、本地消息表、MQ、Seata AT）
- 核心概念和适用场景
- 方案选择决策树

**Java 8 特性（中高信心）**
- Lambda 表达式基本语法
- Stream API 基本用法
- Optional 概念

**Java 版本演进（中信心）**
- 各版本核心特性
- 大厂使用情况
- 版本选择建议

**Java 11 新特性（中信心）**
- String 增强方法
- HTTP Client 基本用法

**Java 17 特性（中信心）**
- Record 类概念和使用方式
- Switch 表达式
- 文本块

### ⚠️ 需要加强

**Stream 流（需要实践）**
- 中间操作和终止操作的深入理解
- flatMap 的使用场景
- 并行流的使用

**Lambda 表达式（需要实践）**
- 函数式接口的深入理解
- 方法引用的使用
- Lambda 的限制和注意事项

**HTTP Client（需要实践）**
- 异步请求的使用
- 错误处理和重试机制
- 实际项目中的应用

**Java 21 特性（需要学习）**
- 虚拟线程的原理和使用
- 模式匹配的高级用法

---

## 关键记忆点

### 分布式事务

**方案选择决策树：**
```
是否涉及金额/库存？
├─ 是 → 是否短事务（1-3个服务）？
│   ├─ 是 → 是否只涉及数据库？
│   │   ├─ 是 → Seata AT（零侵入）
│   │   └─ 否 → TCC（强一致性）
│   └─ 否 → SAGA（长事务）
│
└─ 否 → 是否高并发？
    ├─ 是 → MQ 事务消息
    └─ 否 → 本地消息表
```

---

### Stream 流

**核心概念：**
- 中间操作：懒加载，返回 Stream
- 终止操作：触发执行，返回结果
- 执行顺序：一个元素走完整个流水线

**常用操作：**
```java
list.stream()
    .filter(x -> x > 10)      // 过滤
    .map(x -> x * 2)          // 转换
    .sorted()                 // 排序
    .distinct()               // 去重
    .limit(10)                // 限制
    .collect(Collectors.toList());  // 收集
```

---

### Lambda 表达式

**语法：**
```java
(参数) -> { 方法体 }
```

**函数式接口：**
- `Consumer<T>` - `forEach`
- `Function<T,R>` - `map`
- `Predicate<T>` - `filter`
- `Supplier<T>` - 延迟计算

**方法引用：**
- `Integer::parseInt` - 静态方法
- `System.out::println` - 实例方法
- `User::getName` - 对象方法
- `User::new` - 构造方法

---

### Java 版本

**LTS 版本：**
- Java 8 (2014) - Lambda、Stream、Optional
- Java 11 (2018) - String 增强、HTTP Client
- Java 17 (2021) - Record、Switch 表达式、文本块
- Java 21 (2023) - 虚拟线程

**学习重点：**
1. ✅ Java 8 - 必须掌握
2. ✅ Java 11 - 常用
3. ✅ Java 17 - 推荐
4. ⚠️ Java 21 - 了解

---

### Java 11 String 增强

| 方法 | 作用 | 记忆口诀 |
|------|------|---------|
| `isBlank()` | 判断是否为空或只有空白 | 空白也算空 |
| `lines()` | 按行分割 | 返回 Stream |
| `repeat(n)` | 重复 n 次 | 简单重复 |
| `strip()` | 去除首尾空白 | 比 trim 强 |

---

### HTTP Client

**基本步骤：**
```java
// 1. 创建 HttpClient
HttpClient client = HttpClient.newHttpClient();

// 2. 创建 HttpRequest
HttpRequest request = HttpRequest.newBuilder()
    .uri(URI.create("https://api.example.com"))
    .build();

// 3. 发送请求
HttpResponse<String> response = client.send(
    request, 
    HttpResponse.BodyHandlers.ofString()
);

// 4. 处理响应
System.out.println(response.body());
```

---

## 下次学习建议

### 高优先级

1. **实践 Stream 流**
   - 写代码练习 filter、map、collect
   - 理解中间操作和终止操作的执行顺序
   - 尝试 flatMap、reduce 等高级操作

2. **实践 Lambda 表达式**
   - 写代码练习各种函数式接口
   - 尝试方法引用
   - 理解 Lambda 的限制

3. **实践 HTTP Client**
   - 写代码调用真实的 API
   - 尝试异步请求
   - 实现错误处理和重试机制

### 中优先级

4. **继续学习 Java 8+ 新特性**
   - Optional 的深入使用
   - 新日期时间 API
   - 接口默认方法

5. **学习核心 API**
   - String/StringBuilder/StringBuffer
   - 包装类
   - 日期时间 API

6. **补充集合框架**
   - HashSet 去重原理
   - ConcurrentHashMap
   - Comparable vs Comparator

---

## 总结

今天的学习非常充实！完成了：
1. ✅ 分布式事务完整总结
2. ✅ Stream 流详解
3. ✅ Java 版本演进（Java 8-24）
4. ✅ Lambda 表达式详解
5. ✅ Java 11 新特性（String 增强、HTTP Client）
6. ✅ Record 类使用方式
7. ✅ 大厂 JDK 使用情况

学生表现优秀，学习主动性强，提出了很多好问题。建议下次学习多写代码实践，加深对 Stream、Lambda、HTTP Client 的理解。

**学习时长：** 约 90 分钟  
**掌握主题：** 7 个  
**新增知识点：** Stream 流、Lambda 表达式、Java 11 新特性

继续保持这个学习节奏！💪
